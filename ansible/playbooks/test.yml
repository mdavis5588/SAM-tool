The task includes an option with an undefined variable. The error was: {'sid': '{{ item.sid }}', 'nup_total_users': '{{ (_nup.nup_total_users  | default(0)) | int }}', 'nup_active_users': '{{ (_nup.nup_active_users | default(0)) | int }}', 'nup_locked_users': '{{ (_nup.nup_locked_users | default(0)) | int }}', 'nup_sample_users': '{{ _nup.nup_sample_users  | default([]) }}', 'pdbs': '{{ _pdb_raw.splitlines()      | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}', 'rac_nodes': '{{ _rac_raw.splitlines()      | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}', 'feature_usage': '{{ _feat_raw.splitlines()     | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}', 'pdb_feature_usage': '{{ _pdb_feat_raw.splitlines() | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}'}: 'dict object' has no attribute 'splitlines'. 'dict object' has no attribute 'splitlines'. {'sid': '{{ item.sid }}', 'nup_total_users': '{{ (_nup.nup_total_users  | default(0)) | int }}', 'nup_active_users': '{{ (_nup.nup_active_users | default(0)) | int }}', 'nup_locked_users': '{{ (_nup.nup_locked_users | default(0)) | int }}', 'nup_sample_users': '{{ _nup.nup_sample_users  | default([]) }}', 'pdbs': '{{ _pdb_raw.splitlines()      | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}', 'rac_nodes': '{{ _rac_raw.splitlines()      | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}', 'feature_usage': '{{ _feat_raw.splitlines()     | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}', 'pdb_feature_usage': '{{ _pdb_feat_raw.splitlines() | select(\\'match\\', \\'^\\\\{\"\\') | map(\\'from_json\\') | list }}'}: 'dict object' has no attribute 'splitlines'. 'dict object' has no attribute 'splitlines'\n\nThe error appears to be in '/runner/project/test.yaml': line 822, column 7, but may\nbe elsewhere in the file depending on the exact syntax problem.\n\nThe offending line appears to be:\n\n\n    - name: Build extended instance payload list\n      ^ here\n
