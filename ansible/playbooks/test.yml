Assemble SAM discovery payload
 
{
  "msg": "the field 'args' has an invalid value ({'sam_payload': {'run_id': '{{ sam_run_id }}', 'hostname': '{{ inventory_hostname }}', 'fqdn': '{{ ansible_fqdn }}', 'ip_address': \"{{ ansible_default_ipv4.address | default('') }}\", 'os_family': '{{ ansible_os_family }}', 'os_distribution': '{{ ansible_distribution }}', 'os_version': '{{ ansible_distribution_version }}', 'environment': \"{{ sam_environment | default('unknown') }}\", 'cpu_sockets': '{{ sam_cpu_sockets | int }}', 'cpu_cores_per_socket': '{{ sam_cpu_cores_per_socket | int }}', 'cpu_threads_per_core': '{{ sam_cpu_threads_per_core | int }}', 'total_physical_cores': '{{ sam_total_physical_cores | int }}', 'cpu_model': '{{ sam_cpu_model }}', 'cpu_architecture': '{{ sam_cpu_architecture }}', 'virt_type': '{{ sam_virt_type }}', 'virt_role': '{{ sam_virt_role }}', 'is_vmware': '{{ sam_is_vmware | bool }}', 'is_exadata': '{{ sam_is_exadata | bool }}', 'total_ram_mb': '{{ ansible_memtotal_mb | default(0) }}', 'discovered_at': '{{ ansible_date_time.iso8601 }}', 'instances': \"{{\\n  sqlplus_results.results\\n  | selectattr('rc', 'equalto', 0)\\n  | map(attribute='stdout')\\n  | map('trim')\\n  | map('from_json')\\n  | list\\n}}\"}}), and could not be converted to an dict.The error was: Expecting value: line 1 column 1 (char 0)\n\nThe error appears to be in '/runner/project/test.yaml': line 900, column 7, but may\nbe elsewhere in the file depending on the exact syntax problem.\n\nThe offending line appears to be:\n\n    # ------------------------------------------------------------------\n    - name: Assemble SAM discovery payload\n      ^ here\n",
  "_ansible_no_log": false
}
