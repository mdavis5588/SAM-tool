the field 'args' has an invalid value ({'sam_product_usage_rows': '{{\\n  product_usage_results.results | default([])\\n  | map(attribute=\\'stdout\\') | map(\\'trim\\')\\n  | map(\\'split\\', \\'\\\\n\\') | flatten\\n  | map(\\'trim\\') | select(\\'match\\', \\'^\\\\{\\\\\"\\')\\n  | map(\\'from_json\\')\\n  | list\\n}}'}), and could not be converted to an dict.The error was: Extra data: line 2 column 1 (char 58)\n\nThe error appears to be in '/runner/project/test.yaml': line 928, column 7, but may\nbe elsewhere in the file depending on the exact syntax problem.\n\nThe offending line appears to be:\n\n    # ------------------------------------------------------------------\n    - name: Collect all product_usage rows across instances\n      ^ here\n
